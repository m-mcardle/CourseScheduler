user www-data;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

http {

        ##
        # Basic Settings
        ##

        server {
                listen 80;
                server_name 20.127.99.79;
                location / {
                        proxy_pass http://localhost:3000;
                }
        }
}
